opencv-python>=4.8.0
numpy>=1.24.0
PyYAML>=6.0

# Optional — for YOLO upgrade path (install only when needed):
# ultralytics>=8.0.0

# Optional — for CNN patch classifier upgrade (install only when needed):
# onnxruntime>=1.16.0
